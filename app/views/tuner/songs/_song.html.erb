<tr>
  <td><%= link_to song.name, tuner_song_path(song) %></td>
  <td><%= song.album.artist.name %></td>
  <td><%= song.album.name %></td>
  <td><%= song.duration %></td>
  <td><%= song.genre.name %></td>
  <td><%= link_to 'Favorite', tuner_favorites_path(song_id: song.id), method: :post %></td>
</tr>
